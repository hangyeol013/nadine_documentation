[project]
name = "nadine-documentation"
version = "0.1.0"
description = "Documentation for the Nadine social robot project"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mkdocs>=1.0",
    "mkdocs-material",
    "mkdocs-with-pdf>=0.9.3",
]
